const subjects = ["discrete math", "computer science", 
"algorithms", "data structures", "logic"]

subjects.forEach((subject, index) => {
    const num = index + 1
    console.log(`${num}: ${subject}`)

})